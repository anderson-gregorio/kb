# CLI - Como configura o gerenciador de pacote?

```javascript
// Exibe a configuração atual
ng  config -g cli.packageManager

// Configura para Yarn
ng config -g cli.packageManager yarn

// Configura para NPM
ng config -g cli.packageManager npm
```